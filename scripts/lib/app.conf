# App-specific deployment configuration — timesheet (PayScale)
APP_SLUG="${APP_SLUG:-timesheet}"
APP_PORT="${APP_PORT:-5300}"
NODE_VERSION="${NODE_VERSION:-24}"
PACKAGE_MANAGER="${PACKAGE_MANAGER:-pnpm}"
SYSTEMD_UNIT="${SYSTEMD_UNIT:-timesheet-api}"
HEALTH_PATH="${HEALTH_PATH:-/api/healthz}"
DB_ENGINE="${DB_ENGINE:-postgresql}"
DB_NAME_DEFAULT="${DB_NAME_DEFAULT:-timesheet}"

API_WORKDIR_REL="."
API_START_CMD="node --enable-source-maps artifacts/api-server/dist/index.mjs"
WEB_ROOT_REL="artifacts/payroll-hrms/dist/public"
UPLOAD_SYMLINK_REL=""
MIGRATION_CMD="pnpm --filter @workspace/db run push"
FULL_PNPM_INSTALL="${FULL_PNPM_INSTALL:-true}"
CLIENT_MAX_BODY_SIZE="${CLIENT_MAX_BODY_SIZE:-20m}"
ENABLE_WEBSOCKET="${ENABLE_WEBSOCKET:-false}"
